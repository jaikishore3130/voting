import 'package:flutter/material.dart';

class ElectionInfoTab extends StatelessWidget {
  const ElectionInfoTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Ongoing & Upcoming Elections"),
    );
  }
}
