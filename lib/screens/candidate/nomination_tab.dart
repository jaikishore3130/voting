import 'package:flutter/material.dart';

class NominationTab extends StatelessWidget {
  const NominationTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Track Nomination Status"),
    );
  }
}
