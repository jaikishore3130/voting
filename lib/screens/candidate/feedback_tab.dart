import 'package:flutter/material.dart';

class FeedbackTab extends StatelessWidget {
  const FeedbackTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Voter Feedback & Suggestions"),
    );
  }
}
