import 'package:flutter/material.dart';

class CampaignTab extends StatelessWidget {
  const CampaignTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Manage Campaign Strategies"),
    );
  }
}
